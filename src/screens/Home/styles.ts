import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },

  helloWorld: {
    fontSize: 28,
    paddingRight: 20,
    textAlign: 'center',
    color: '#2c2c2c',
  },
});
